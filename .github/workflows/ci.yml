name: CI

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  wdem-cross-platform:
    name: WDEM portable format and build
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
    - uses: actions/checkout@v7

    - name: Setup .NET
      uses: actions/setup-dotnet@v6
      with:
        dotnet-version: 10.0.x

    - name: Setup uv
      uses: astral-sh/setup-uv@v7

    - name: Setup bun
      uses: oven-sh/setup-bun@v2

    - name: Restore portable WDEM projects
      run: dotnet restore tests/Wdem.Core.Tests/Wdem.Core.Tests.csproj -m:1

    - name: Check portable WDEM formatting
      run: dotnet format tests/Wdem.Core.Tests/Wdem.Core.Tests.csproj --verify-no-changes --verbosity diagnostic --no-restore

    - name: Check Python formatting
      run: |
        uv tool run ruff check plugins/
        uv tool run ruff format --check plugins/

    - name: Check JS/TS formatting
      run: bun x prettier --check plugins/

    - name: Build portable WDEM projects
      run: dotnet build tests/Wdem.Core.Tests/Wdem.Core.Tests.csproj --no-restore -m:1

  wdem-windows:
    name: WDEM Windows tests
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v7

    - name: Setup .NET
      uses: actions/setup-dotnet@v6
      with:
        dotnet-version: 10.0.x

    - name: Restore dependencies
      run: dotnet restore Wdem.sln -m:1 -p:EnableWindowsTargeting=true

    - name: Verify WDEM product identity
      shell: pwsh
      run: ./testing/wdem/assert-product-identity.ps1

    - name: Check WDEM formatting
      run: dotnet format Wdem.sln --verify-no-changes --verbosity diagnostic --no-restore

    - name: Build solution
      run: dotnet build Wdem.sln --no-restore -m:1 -p:EnableWindowsTargeting=true

    - name: Install uv and bun
      shell: pwsh
      run: |
        powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
        echo "$env:USERPROFILE\.cargo\bin" >> $env:GITHUB_PATH
        powershell -ExecutionPolicy ByPass -c "irm https://bun.sh/install.ps1 | iex"
        echo "$env:USERPROFILE\.bun\bin" >> $env:GITHUB_PATH

    - name: Run WDEM tests
      run: dotnet test Wdem.sln --no-restore --verbosity normal --collect "XPlat Code Coverage" -m:1 -p:EnableWindowsTargeting=true

    - name: Test plugin action downloads
      shell: bash
      run: |
        echo "Testing source-derived plugin action downloads..."
        cd plugins
        TOTAL=0
        FAILED=0

        for plugin_dir in */; do
          plugin_name=$(basename "${plugin_dir}")
          if [[ "$plugin_name" == "__pycache__" || "$plugin_name" == "." ]]; then
            continue
          fi

          cd "$plugin_dir"
          TOTAL=$((TOTAL + 1))

          if [[ -f "plugin.yaml" ]]; then
            # For Python plugins
            if grep -q '"type: python"' plugin.yaml; then
              if [[ -f "src/plugin.py" ]]; then
                echo "✓ $plugin_name: Python plugin exists"
              else
                echo "❌ $plugin_name: Python plugin exists but missing src/plugin.py"
                FAILED=$((FAILED + 1))
              fi
            # For JavaScript/TypeScript plugins
            elif grep -q '"type: javascript"' plugin.yaml || grep -q '"type: typescript"' plugin.yaml; then
              if [[ -f "src/plugin.ts" ]] || [[ -f "src/plugin.js" ]] || [[ -f "package.json" ]]; then
                echo "✓ $plugin_name: JavaScript plugin exists"
              else
                echo "❌ $plugin_name: JavaScript plugin exists but missing src/plugin and package.json"
                FAILED=$((FAILED + 1))
              fi
            # For PowerShell plugins
            elif grep -q '"type: powershell"' plugin.yaml; then
              if [[ -f "src/plugin.ps1" ]]; then
                echo "✓ $plugin_name: PowerShell plugin exists"
              else
                echo "❌ $plugin_name: PowerShell plugin exists but missing src/plugin.ps1"
                FAILED=$((FAILED + 1))
              fi
            else
              echo "⚠️  $plugin_name: Unknown plugin type"
            fi
          else
            echo "⚠️  $plugin_name: No plugin.yaml found (test plugin)"
          fi

          cd ..
        done

        echo "Plugin action download test completed."
        echo "Total plugins tested: $TOTAL"
        echo "Failed plugins: $FAILED"

        if [[ $FAILED -gt 0 ]]; then
          echo "❌ $FAILED plugin(s) failed action download tests"
          exit 1
        else
          echo "✅ All $TOTAL plugin action download tests passed"
        fi
