<UserControl x:Class="Wdem.Desktop.Views.ResourceSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <DataTemplate x:Key="ResourceItemTemplate">
            <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}"
                      IsEnabled="{Binding CanChangeSelection}"
                      HorizontalContentAlignment="Stretch">
                <Grid MinWidth="420" Margin="4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Spacing="2">
                        <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding Description}" Opacity="0.72" />
                    </StackPanel>
                    <TextBlock Grid.Column="1"
                               Margin="16,0,0,0"
                               Text="{Binding OriginDisplayName}"
                               VerticalAlignment="Center" />
                </Grid>
            </CheckBox>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Padding="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="8">
            <TextBlock Text="选择资源" Style="{StaticResource SectionTitleStyle}" />
            <TextBlock Text="{Binding ProfileNameDisplay}" />
        </StackPanel>

        <ScrollViewer Grid.Row="1" Margin="0,24,0,24">
            <StackPanel Spacing="20">
                <StackPanel>
                    <TextBlock Text="Required" Style="{StaticResource ResourceGroupTitleStyle}" />
                    <ListView ItemsSource="{Binding RequiredResources}"
                              ItemTemplate="{StaticResource ResourceItemTemplate}"
                              SelectionMode="None" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="Optional" Style="{StaticResource ResourceGroupTitleStyle}" />
                    <ListView ItemsSource="{Binding OptionalResources}"
                              ItemTemplate="{StaticResource ResourceItemTemplate}"
                              SelectionMode="None" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="Auto dependency" Style="{StaticResource ResourceGroupTitleStyle}" />
                    <ListView ItemsSource="{Binding AutoDependencies}"
                              ItemTemplate="{StaticResource ResourceItemTemplate}"
                              SelectionMode="None" />
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="12">
            <Button Content="检查环境" Command="{Binding CheckEnvironmentCommand}" />
            <Button Content="开始配置" Command="{Binding StartConfigurationCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
