# Shell completion

WDEM has no public command-line host yet, so no supported shell-completion
installation exists. Completion generation in `Wdem.LegacySource` is retained
as transition-library behavior and uses the future `wdem` command name.

End-user completion instructions will be published with `Wdem.Cli`.
